name: Validate Template

on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
  validate-template:
    runs-on: ubuntu-latest
    steps:
      - name: Placeholder
        run: echo "validate-template workflow placeholder"
